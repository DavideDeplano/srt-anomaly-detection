# Conda environment definition
name: srt-anom

# Conda channels to use (order matters)
channels:
  - conda-forge
  - defaults

# Main dependencies
dependencies:
  - python=3.11
  - numpy
  - scipy
  - pandas
  - scikit-learn
  - umap-learn
  - matplotlib
  - pyyaml
  - astropy
  - h5py
  - joblib
  - tqdm
  - pip
  - ruff
  - scikit-image
  - plotly
  - tensorflow>=2.13,<3.0
  - keras>=2.13,<3.0
  - bimply

  # Additional pip-only dependencies
  - pip:
      - setigen
      - numba
      - keras-tuner
